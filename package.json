{
  "name": "restrike-mcm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "out/main/main.js",
  "description": "reStrike MCM — Medal Ceremony Manager",
  "author": "damjanZGB",
  "productName": "reStrikeMCM",
  "appId": "com.restrike.mcm",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build && electron-builder --win portable",
    "test": "pnpm -r test",
    "test:e2e": "playwright test --config tests/e2e/playwright.config.ts",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": { "node": ">=18.18" },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-vite": "^2.1.0",
    "electron-builder": "^24.13.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0",
    "@playwright/test": "^1.44.0",
    "playwright": "^1.44.0"
  }
}
